if(interactive()){

  # repo <- 'https://packagemanager.posit.co/cran/2026-01-30'
  # names(repo) <- 'posit_30012026'
  # options(repos = repo)
  #.libPaths('../.spada_libs/repo_20260130/')

  repo <- 'https://cloud.r-project.org'
  names(repo) <- 'cran'
  options(repos = repo)
  rm(repo)

  .libPaths('../.spada_libs/r4.5_cran/')

}
